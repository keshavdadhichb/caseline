[pytest]
pythonpath = .
markers =
    live: real network calls to the Anthropic API (costs tokens, needs ANTHROPIC_API_KEY) — excluded by default, run explicitly with `pytest -m live` (see Makefile's `test-live` target)
addopts = -m "not live"
