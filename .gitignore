# --- NEVER COMMIT: contest-confidential context ---
HACKATHON.md

# --- Secrets ---
.env
.env.*
!.env.example

# --- Data: raw Kaggle downloads are too big & must not be pushed ---
data/raw/
*.zip

# --- Caches / runtime ---
# .cache/plans/ is INTENTIONALLY NOT ignored — pre-warmed planner responses
# are committed so the demo works offline (CLAUDE.md "Resilience & demo
# insurance"). Ignore everything else under .cache/.
.cache/*
!.cache/plans/
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# --- Frontend ---
node_modules/
frontend/dist/

# --- OS / editor ---
.DS_Store
.claude/
